import {Router} from "express";import "../../container";import {container} from "tsyringe";import {WalletController} from "./wallet.controller";import {requireVendorAuth} from "../auth/vendor/vendor-auth.middleware";import {dtoValidation} from "../../middlewares/dtoValidation";import {WalletWithdrawalDto,WalletIssueDto} from "./wallet.dto";
const r=Router(),c=container.resolve(WalletController);r.use(requireVendorAuth);r.get("/",c.get);r.get("/history",c.history);r.post("/withdraw",dtoValidation(WalletWithdrawalDto),c.withdraw);r.post("/issues",dtoValidation(WalletIssueDto),c.issue);export default r;
