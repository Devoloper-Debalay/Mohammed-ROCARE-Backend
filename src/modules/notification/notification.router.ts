import {Router} from "express";import "../../container";import {container} from "tsyringe";import {NotificationController} from "./notification.controller";import {requireCustomerAuth} from "../customer/customer.middleware";import {requireVendorAuth} from "../auth/vendor/vendor-auth.middleware";
const r=Router(),c=container.resolve(NotificationController);r.get("/customer",requireCustomerAuth,c.user);r.patch("/customer/:id/read",requireCustomerAuth,c.readUser);r.get("/vendor",requireVendorAuth,c.vendor);r.patch("/vendor/:id/read",requireVendorAuth,c.readVendor);export default r;
